# Force rebuild: 2026-01-23-1803
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements-deploy.txt"]

[start]
cmd = "python run.py"

[variables]
PYTHON_VERSION = "3.11"
PYTHONPATH = "/app"
